バブルソート・・・隣り合う値同士の大小関係を見て、入れ替えて行く。

■ソースコードのポイント
隣り合う値を見て入れ替える処理と、ソート済みの２つのリストをマージする処理を
merge関数でいっぺんに実装している点。
マージ処理の入力となる両リストが共に長さ１の時は、入れ替え処理に相当する。

長いリストを、まず半分にして、片方を更に半分、更に片方を半分に、、、と、
要素数が２になるまで割ることで、隣り合う値同士の入れ替え処理に持って行っている。









